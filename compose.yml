# Docker Compose - PostgreSQL + API
# maskara dev  -> docker compose up postgres -d  (database only)
# maskara up   -> docker compose up --build      (full stack)

services:
  postgres:
    image: pgvector/pgvector:pg18
    container_name: maskara-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-maskara}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-maskara_secret}
      POSTGRES_DB: ${POSTGRES_DB:-maskara}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-maskara}"]
      interval: 5s
      timeout: 5s
      retries: 5

  maskara:
    build: .
    container_name: maskara-api
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-maskara}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-maskara_secret}
      - POSTGRES_HOST=postgres
      - POSTGRES_DB=${POSTGRES_DB:-maskara}
      - AEGRA_CONFIG=aegra.json
      - AUTH_TYPE=${AUTH_TYPE:-noop}
      - PORT=${PORT:-8000}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      start_period: 10s
    volumes:
      - ./src:/app/src:ro
      - ./maskara.json:/app/maskara.json:ro

volumes:
  postgres_data:
